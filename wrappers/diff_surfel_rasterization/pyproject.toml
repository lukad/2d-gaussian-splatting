[build-system]
requires = [
  "setuptools>=70.1",
  "wheel",
  "numpy",
  "torch",
]
build-backend = "setuptools.build_meta"

[project]
name = "diff_surfel_rasterization"  # or simple_knn
version = "0.0.1"
dependencies = ["torch"]
